<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BREAD (Browse, Read, Edit, Add, Delete) and Pagination</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h1>SQlite BREAD (Browse, Read, Edit, Add, Delete) and Pagination</h1>
            </div>
            <div class="card-body">
                <form action="/search" method="get" class="search-form">
                    <div class="form-group">
                        <label>Name</label>
                        <input type="text" name="name" id="name" placeholder="Search Name">
                    </div>
                    <div class="form-group">
                        <label for="height">Height</label>
                        <input type="number" name="height" id="height" placeholder="Search Height">
                    </div>
                    <div class="form-group">
                        <label for="weight">Weight</label>
                        <input type="number" step="0.01" min="0" name="weight" id="weight"
                            placeholder="Insert Your Weight">
                    </div>
                    <div class="form-group">
                        <label for="birthDateStart">Birth Date</label>
                        <input type="date" name="birthDateStart" id="birthDateStart">
                        <span>s.d.</span>
                        <input type="date" name="birthDateEnd" id="birthDateEnd">
                    </div>
                    <div class="form-group">
                        <label for="isMarried">Is Married</label>
                        <select name="isMarried" id="isMarried">
                            <option value="" disabled selected>-select married-</option>
                            <option value="true">Yes</option>
                            <option value="false">Not Yet</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>Operation</label>
                        <div class="form-check">
                            <input type="radio" name="operation" value="OR" checked> OR
                            <input type="radio" name="operation" value="AND"> AND
                        </div>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="search-button">Search</button>
                        <button type="button" class="reset-button"
                            onclick="window.location.href='/reset';">Reset</button>
                    </div>
                </form>
            </div>
        </div>

        <div>
            <table>
                <thead>
                    <tr>
                        <th>NO.</th>
                        <th>name</th>
                        <th>height</th>
                        <th>weight</th>
                        <th>birth Date</th>
                        <th>Is Married</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach((person, index)=> { %>
                        <tr>
                            <th>
                                <%= index + 1 %>
                            </th>
                            <td>
                                <%= person.name %>
                            </td>
                            <td>
                                <%= person.height %>
                            </td>
                            <td>
                                <%= person.weight %>
                            </td>
                            <td>
                                <%= person.birthdate %>
                            </td>
                            <td>
                                <%= person.married ? "Yes" : "Not Yet" %>
                            </td>
                            <td>
                                <a href="/update/<%= person.id %>" class="update-button">Update</a>
                                <a href="/delete/<%= person.id %>" class="delete-button">Delete</a>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
        <div class="card-footer">
            <a href="/add">Add</a>

            <!-- Tombol Previous (Sebelumnya) -->
            <% if (page> 1) { %>
                <a href="/?page=<%= page - 1 %>&search=<%= searchQuery %>" class="page-button">⟪</a>
                <% } else { %>
                    <span class="page-button disabled">⟪</span>
                    <% } %>

                        <!-- Halaman yang aktif dan tombol halaman lainnya -->
                        <% for (let i=1; i <=totalPages; i++) { %>
                            <a href="/?page=<%= i %>&search=<%= searchQuery %>"
                                class="page-button <%= page === i ? 'active' : '' %>">
                                <%= i %>
                            </a>
                            <% } %>

                                <!-- Tombol Next (Berikutnya) -->
                                <% if (page < totalPages) { %>
                                    <a href="/?page=<%= page + 1 %>&search=<%= searchQuery %>" class="page-button">⟫</a>
                                    <% } else { %>
                                        <span class="page-button disabled">⟫</span>
                                        <% } %>
        </div>

    </div>
    </div>
    </div>
    </div>
</body>

</html>